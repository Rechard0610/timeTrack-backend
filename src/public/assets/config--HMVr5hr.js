const t=new Date,a=new Date(t);a.setDate(t.getDate()-t.getDay()+(t.getDay()===0?-6:1));const n=new Date(t);n.setDate(a.getDate()+6);const o=[];for(let e=new Date(a);e<=n;e.setDate(e.getDate()+1))o.push(new Date(e));const r={userId:{label:"member",type:"avatar"},total:{label:"total",type:"string"},...Object.fromEntries(o.map(e=>[e.toLocaleString("en-US",{weekday:"short"}).toLowerCase(),{label:`${e.toLocaleString("en-US",{weekday:"long"})} ${e.toLocaleDateString("en-US",{year:"numeric",month:"2-digit",day:"2-digit"})}`,type:"editable"}]))};export{r as l};
